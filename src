import React from 'react';
import ReactDOM from 'react-dom/client';
import PizzamicaMejorada from './menu-pizzamica'; // Asegúrate de que el nombre coincida con tu archivo .jsx

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PizzamicaMejorada />
  </React.StrictMode>
); 
